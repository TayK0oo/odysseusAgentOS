"""Odysseus data pipelines — Prefect-based scheduled flows.

Activate with ODYSSEUS_PREFECT=on (kill-switch). Without Prefect installed,
all imports raise ImportError and the built-in task_scheduler takes over.
"""
