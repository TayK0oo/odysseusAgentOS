# Phase 5.2 — Activer Phase-lock + RRF

**Milestone :** M5 — Activation | **Phase :** 5.2 | **Autonomous :** false
**Dépendances :** 5.1 (dépendances)
**Effort :** 2h

---

## Objectif

Activer le PhaseTracker et le RRF fusion, valider le comportement.

## Kill-switches à activer

```bash
ODYSSEUS_PHASE_TRACKER=on
ODYSSEUS_RRF_FUSION=on
```

## Fichiers de référence

| Fichier | Contenu |
|---------|---------|
| `src/orchestrator/phase_tracker.py:53` | PhaseTracker — pont loop live ↔ phase-lock |
| `config/phase-lock.yaml:114` | 10 phases avec restrictions outils |
| `src/rag_vector.py:50` | RRF fusion — hybride vecteur + BM25 |
| `docs/architecture/agent-loop.md` | Doc boucle canonique + phase-lock |

## Actions

1. Ajouter `ODYSSEUS_PHASE_TRACKER=on` dans `.env`
2. Ajouter `ODYSSEUS_RRF_FUSION=on` dans `.env`
3. Redémarrer l'app
4. Vérifier : `curl /api/phase/current` retourne la phase
5. Vérifier : `curl /api/killswitches` montre PHASE_TRACKER=on, RRF_FUSION=on
6. Benchmark RAG : RRF vs naive blend (précision, recall)

## Validation

- [ ] PhaseTracker infère la phase par round
- [ ] Phase-lock bloque les outils hors phase
- [ ] RRF fusion ≥ naive blend (précision)
- [ ] 0 régression de test

## Références croisées

- → Phase 5.3 (Mem0)
- → `docs/architecture/agent-loop.md`
- → `docs/security/auth-permissions.md`
