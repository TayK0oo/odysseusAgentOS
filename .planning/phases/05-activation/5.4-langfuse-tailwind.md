# Phase 5.4 — Activer LangFuse + Tailwind CSS

**Milestone :** M5 — Activation | **Phase :** 5.4 | **Autonomous :** false
**Dépendances :** 5.3 (Mem0 + Meilisearch)
**Effort :** 4h

---

## Objectif

Activer LangFuse (LLM tracing) et intégrer Tailwind CSS dans l'UI.

## Kill-switches à activer

```bash
ODYSSEUS_LANGFUSE=on
```

## Services Docker à démarrer

```bash
docker compose --profile observability up -d
```

## Fichiers de référence

| Fichier | Contenu |
|---------|---------|
| `services/observability/langfuse_tracer.py:161` | Tracer LangFuse avec @observe() |
| `services/observability/otel_setup.py:159` | OpenTelemetry setup |
| `static/tailwind.css` | Source Tailwind v4 |
| `static/css/style.tailwind.min.css` | CSS compilé (16.3 KB) |
| `tailwind.config.js` | Configuration Odysseus dark theme |
| `docker-compose.yml` | Profil observability (langfuse + postgres) |
| `docs/operations/observability.md` | Guide LangFuse |

## Actions

1. Démarrer LangFuse : `docker compose --profile observability up -d`
2. Vérifier : `curl http://localhost:3000` (dashboard LangFuse)
3. Envoyer un message chat → trace visible dans LangFuse
4. Intégrer `style.tailwind.min.css` dans `static/index.html`
5. Vérifier visuellement : dark theme intact, composants OK

## Validation

- [ ] Dashboard LangFuse accessible sur :3000
- [ ] Traces LLM visibles (tokens, coût, latence)
- [ ] Tailwind CSS chargé dans index.html
- [ ] UI visuellement identique (dark theme, Fira Code)
- [ ] Taille CSS : 16.3 KB (vs 1,220 KB avant)

## Références croisées

- → Phase 5.5 (Gouvernance + Autoeval)
- → `docs/operations/observability.md`
- → `docs/development/guidelines.md`
