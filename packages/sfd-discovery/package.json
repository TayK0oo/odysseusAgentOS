{
  "name": "@agentos/sfd-discovery",
  "private": true,
  "version": "0.1.0",
  "description": "SFD Discovery — service registry, endpoint probing, connector detection",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@opencode-ai/plugin": "latest"
  }
}
